export default function LayoutTeachers({
    children
}) {
    return (
        <section>
            <h1 style={{textAlign: 'center'}}>Teachers Page</h1>
            {children}
        </section>
    )
}